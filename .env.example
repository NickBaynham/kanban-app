# OpenRouter API key — required for the AI chat endpoint
OPENROUTER_API_KEY="sk-or-v1-..."

# JWT signing secret — REQUIRED in production. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=""

# Database connection. The Docker compose stack sets this automatically;
# uncomment for local dev against a non-default Postgres.
# DATABASE_URL="postgresql://kanban:kanban@localhost:5432/kanbandb"
